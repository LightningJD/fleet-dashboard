{
  "name": "lightning-fleet-market-intel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static fleet dashboard plus market intelligence scripts for rental/Turo operations.",
  "scripts": {
    "serve": "python3 -m http.server 5173",
    "validate": "node scripts/validate-data.mjs",
    "collect:market": "node scripts/collect-market.mjs",
    "validate:market": "node scripts/validate-market-snapshots.mjs",
    "analyze": "node scripts/pricing-engine.mjs",
    "check": "npm run validate && npm run validate:market && npm run analyze && node scripts/check-app.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.49.1"
  }
}
